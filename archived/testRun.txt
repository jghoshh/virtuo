	// -------------------------
	// TEST RUN
	// -------------------------
	username := "testuser"
	email := "testuser@example.com"
    password := "testpassword123"
	token, refresh, err := client.SignUp(username, email, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	keyring.Delete("Virtuo", authToken)
	keyring.Delete("Virtuo", authTokenRefresh)
	token, refresh, err = client.SignUp(username, email, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	keyring.Delete("Virtuo", authToken)
	keyring.Delete("Virtuo", authTokenRefresh)
	token, refresh, err = client.SignIn(username, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	token, refresh, err = client.SignIn(username, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	time.Sleep(7 * time.Second)
	err = client.UpdateUser(password, username, email, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	token, refresh, err = client.SignIn(username, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	keyring.Delete("Virtuo", authToken)
	keyring.Delete("Virtuo", authTokenRefresh)
	err = client.SignOut()
	if err != nil {
		client.PrintError(err.Error())
	}
	token, refresh, err = client.SignUp(username, email, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	err = client.SignOut()
	if err != nil {
		client.PrintError(err.Error())
	}
	token, refresh, err = client.SignUp(username, email, password)
	if err != nil {
		client.PrintError(err.Error())
	}
	fmt.Println(token)
	fmt.Println(refresh)
	err = client.DeleteUser()
	if err != nil {
		client.PrintError(err.Error())
	}